export type UserDto = {
    name: string | null,
    password?: string | null,
    email: string | null,
    birthDate: string | null,
    pseudo: string | null,
    bio: string | null,
    firstname: string,
    profilePicUri: string | null
}